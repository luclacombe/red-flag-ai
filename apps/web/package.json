{
  "name": "@redflag/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "lint": "echo 'web lint: deferred to phase 0.6'",
    "type-check": "echo 'web type-check: deferred to phase 0.6'",
    "test": "echo 'web test: deferred to phase 0.9'",
    "build": "echo 'web build: deferred to phase 0.6'"
  }
}
